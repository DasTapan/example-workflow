name: Sedstart Cloud 

on:
  workflow_dispatch:

jobs:
  sedstart-run:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger SedStart test run
        uses: DasTapan09/runcloud@ss-813
        with:
          api_key: ${{ secrets.SEDSTART_API }}
          project_id: 364
          test_id: 2397
          browser: "chrome"
          environment: "QA"
