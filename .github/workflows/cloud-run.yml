name: Sedstart Cloud 

on:
  workflow_dispatch:

jobs:
  sedstart-run:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger SedStart test run
        uses: sedstart/runlocal
        with:
          api_key: ${{ secrets.SEDSTART_API }}
          project_id: 364
          test_id: 1629
          suite_id: 316
          browser: "chrome"
          environment: "QA"
