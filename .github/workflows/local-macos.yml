name: Sedstart Local macOS

on:
  workflow_dispatch:

jobs:
  sedstart-run:
    runs-on: macos-15

    steps:
      - name: Trigger SedStart test run
        uses: DasTapan09/runlocal@feature
        with:
          api_key: ${{ secrets.SEDSTART_API }}
          project_id: 364
          test_id: 1629
          profile_id: 616
          browser: "safari"
          environment: QA
